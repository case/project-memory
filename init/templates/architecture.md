---
title: Architecture
summary: <One-line summary of stack + hosting>
created: ${today}
updated: ${today}
author: ${author}
tags: [architecture, stack, codebase, conventions]
---

# Architecture

The current implementation of ${name}. Replace any of this and the [product](product.md) is unaffected. The product is "above the user experience abstraction layer," and the architecture is "below the abstraction layer."

## Repo layout

<Directory tree — top-level dirs and what they hold.>

## Stack

<Layered table: backend, frontend, database, auth, hosting, observability, etc., with the chosen technology and a one-line note for each.>

## Vendor decisions

<Explicit picks (and explicit "never X" decisions) with one-line rationale each.>

## Conventions

<How we do things: version pinning, linting, testing, git hooks, CI runner, deploy flow.>
