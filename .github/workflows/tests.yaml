name: Tests

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-slim

    steps:
      - name: Check out repo
        uses: actions/checkout@v6

      - name: Run tests
        run: bin/test
